# MOCA (Marketing On CAfe · 네이버 카페 자동화) 작업 규약 — 시작 전 반드시 읽는다
# ⚠️ 퍼블리와 완전 무관한 독립 앱. 퍼블리 UI/DB 이식 금지, 검증된 기술 베이스만 재활용.

> 이 파일은 매 세션 자동 로드된다. 테리가 반복 지적한 실수(부분수정 회귀·기능설명 누락·전수 미적용)를 막기 위한 **불가침 규약**이다. 아래를 어기면 또 망가진다.

## 🚨 계속 반복된 문제 — 어떤 작업이든 전/후로 이 셋을 자기점검
여러 번 지적했는데 계속 재발했다. 절대 다시 깨지 않는다.

### A. 탭을 옮기면 초기화·중단된다 (가장 많이 지적)
- 실행 중 작업이 **멈추면 안 된다**. 실제 일은 봇(브라우저 창)이 하니, 앱에서 탭을 옮기거나 화면을 내려도 계속돼야 한다. **봇 창만 살아있으면 앱은 내려도 됨.**
- 화면 상태(입력값·로그·진행률·KPI)가 **초기화되면 안 된다**.
- → 실행형/데이터 컴포넌트는 `{tab===x && ...}` 조건부 렌더 **금지**. `visitedAutoTabs`+`display:none` 숨김으로 **언마운트 자체를 막는다**. (트래픽 유입·크롤링·플레이스365·서이추·공감·품앗이 등 전부. 새 탭 추가 시에도 이 방식.)

### B. 과거 데이터가 날아간다 / 안 쌓인다
- KPI·유입·순위·누적은 **Supabase에 영속** 저장 → 앱 껐다 켜도·버전 업데이트해도 유지.
- **무제한(관리자)도 카운트를 기록**해야 '오늘 유입/누적/그래프'가 0으로 안 남는다.
- **기간 고정 금지** — 최근 7일뿐 아니라 **30/90/전체·사용자 기간설정**으로 과거까지 눈으로 본다. 플레이스·블로그 둘 다.
- **대상(매장/블로그)별로 분리** 저장·표시. 대상 바꾸면 화면 전체가 그 대상 데이터로 갱신.

### C. 부분만 고쳐서 짝이 안 맞는다 (회귀의 주원인)
- 회원↔관리자 / 플레이스↔블로그 / 프론트↔봇 — **항상 양쪽 다** 고친다.

## 0. 작업 시작 순서 (예외 없음)
1. 관련 **메모리 먼저 읽는다**(MEMORY.md 인덱스 → 해당 topic). "안 읽고 하다 문제" 반복 금지.
2. **TaskCreate로 체크리스트부터** 만든다. 하나씩 처리, 흘리지 않는다.
3. 하던 작업은 **완결하고** 다음으로. 새 지시는 큐에 적어두고 순서대로.

## 1. 기능설명은 기본값이다 (매번)
- 모든 새 기능·수정에는 **눈에 잘 보이는 곳**에 기능설명을 넣는다(사용법·계산법·로그인 필요여부 등). 사용자가 머리로 계산하게 두지 않는다.
- 좋은 기능도 **활용법을 모르면 죽은 기능**이다. 설명은 선택이 아니라 기본.

## 2. 부분만 고치지 않는다 = 전수 적용 (회귀의 주원인)
하나를 고치면 **짝이 되는 것 전부** 같이 고친다. 한쪽만 고치면 반드시 망가진다:
- **회원 ↔ 관리자** 동일 (DashboardPage ↔ AdminPage). `[[feedback_member_admin_parity]]`
- **플레이스 ↔ 블로그** 동일 (targetType 양쪽 다).
- **프론트 ↔ 서버(봇)** 양쪽 (src ↔ naver-bot).
- **시작 시 로그**에 적용된 설정을 항목별로 전부 찍는다.
- 공용 컴포넌트(InflowCenter 등)는 한 번 고치면 양쪽 반영됨 — 확인하고 중복작업 말 것.

## 3. 불변 원칙 (절대 깨지 않는다)
- **실행 중 기능은 탭 이동·화면 내림·최소화에도 안 꺼진다.** 조건부 렌더(`{tab===x&&}`) 금지, `visitedAutoTabs`+display 숨김. `[[feedback_publy_keep_running_on_tab_switch]]`
- **모든 데이터는 대상(매장/블로그)별로 분리·영속.** 대상 바꾸면 화면 전체 그 데이터로 갱신. 앱 업데이트해도 기록 유지(전부 Supabase). `[[feedback_publy_per_target_data_persist]]`
- **데이터는 기간 고정 금지** — 7일뿐 아니라 전체·기간설정으로 과거까지 본다.
- `window.prompt/confirm`은 **Electron에서 안 뜬다** → 커스텀 모달만.
- 🔴🔴 **봇 창은 항상 작은 창(전체창 금지).** 창 크기는 `naver-bot/src/naver.ts`의 `LAUNCH_ARGS`(WINDOW_W/WINDOW_H, `--window-size`) **단일 소스** 하나뿐. **개별 `chromium.launch`에 `--start-maximized` 절대 붙이지 말 것** — 한 곳만 붙여도 그 기능 경로에서만 전체창으로 회귀한다(테리 여러 번 지적, "수정만 하면 다시 전체창"의 원인). 크기 바꿀 땐 WINDOW_W/H 한 곳만 수정.

## 4. 회귀 방지 (커밋 전 필수)
- 수정 후 **타입체크**: 프론트 `npx tsc --noEmit -p tsconfig.json` + 봇 `cd naver-bot && npx tsc --noEmit`.
- 고친 것의 **연관 기능이 안 깨졌는지** 확인(특히 공용 컴포넌트·공유 상태).
- 검증 없이 "됐다" 단정 금지.

## 5. 배포
- `package.json` **version bump** → 커밋 → `git push origin main` → GitHub Actions 빌드 성공 확인.
- 설치본은 **Releases**에서 받음. CI가 최신 자동 업로드. `[[feedback_publy_release_download]]`
- 봇 로컬검증 시 3333 포트·`pkill -f dist/server.js` 금지(발행 중인 앱봇 죽임). `[[feedback_no_bot_port_collision]]`

## 리포지토리
- 로컬 `~/Downloads/moca`, GitHub **깃허브2 `tarry-2/moca`**(퍼블리와 별개 계정·레포).
- 프론트 `src/`(React+Vite+Electron), 봇 `naver-bot/`(Playwright, 카페 로그인·발행 엔진).
- Supabase = 모카 전용 `dxtxnwlvmtewvvvqpisl`(퍼블리 DB와 공유 금지). 세션키 `MOCA_SESSION_V1`/`~/.moca`.
